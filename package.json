{
  "name": "stromboli-plugin-sass",
  "version": "0.17.0",
  "description": "",
  "main": "src/plugin.js",
  "scripts": {
    "pretest": "npm prune && npm update",
    "test": "tap test/*.js --cov --reporter=spec --100"
  },
  "author": "Eric MORAND",
  "dependencies": {
    "css-region-rebase": "^1.1.0",
    "merge": "^1.2.0",
    "promise": "^7.1.1",
    "stylesheet-deps": "^1.1.0",
    "through2": "^2.0.3"
  },
  "repository": "ericmorand/stromboli-plugin-sass",
  "license": "Apache-2.0",
  "devDependencies": {
    "clean-css": "^3.4.22",
    "node-sass": "^3.0.0",
    "tap": "^7.1.2"
  },
  "peerDependencies": {
    "node-sass": "^3.0.0"
  }
}
